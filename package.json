{
  "name": "beziera",
  "private": true,
  "version": "0.1.0",
  "description": "AI-assisted UI/UX design, prototype and animation platform. HTML-native artboards on a pan/zoom canvas.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@beziera/core && npm run build --workspace=@beziera/mcp && npm run build --workspace=@beziera/canvas",
    "canvas": "npm run start --workspace=@beziera/canvas",
    "mcp": "npm run start --workspace=@beziera/mcp",
    "test": "npm run build && npm run test --workspace=@beziera/core && npm run test --workspace=@beziera/mcp && npm run test --workspace=@beziera/canvas"
  }
}
